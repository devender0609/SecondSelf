{
  "installCommand": "corepack enable && corepack prepare yarn@1.22.22 --activate && yarn install --network-timeout 600000 --ignore-engines",
  "buildCommand": "yarn build",
  "outputDirectory": "dist",
  "framework": "vite"
}